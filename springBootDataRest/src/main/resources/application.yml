spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    driver-class-name: org.h2.Driver
#    url: jdbc:h2:tcp://localhost/mem:testdb5
    url: jdbc:h2:mem:testdb7
#    url: jdbc:h2:file:/tmp/test6
    username: sa
    password:
    initialize: true
    schema: classpath:schema.sql # same as default if file exists
#    data: ...data.sql # same as default if file exists
  h2:
    console:
      enabled: true
      path: /h2


